# Deploy — Конфигурация деплоя фронтенда

## 📁 Структура

```
deploy/
├── docker/
│   ├── frontend/
│   │   └── Dockerfile          # Dockerfile для сборки фронтенда
│   └── nginx/
│       ├── nginx.conf          # Конфигурация Nginx для продакшна
│       └── ssl/
│           └── README.md       # Инструкции по SSL сертификатам
├── docs/
│   ├── DEPLOYMENT_GUIDE.md     # Полное руководство по деплою
│   └── QUICK_REFERENCE.md      # Краткая справка
├── docker-compose.yml          # Базовый docker-compose
├── docker-compose.dev.yml      # Конфигурация для разработки
├── docker-compose.prod.yml     # Конфигурация для продакшна
└── README.md                   # Этот файл
```

## 🚀 Быстрый старт

### Разработка (порт 3000)

```bash
cd deploy
docker-compose -f docker-compose.dev.yml up -d
```

Фронтенд будет доступен на http://localhost:3000

### Продакшн (порт 80/443)

```bash
cd deploy
docker-compose -f docker-compose.prod.yml up -d
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `NEXT_PUBLIC_API_URL` | URL бэкенда API | `http://localhost:8001` |
| `NEXT_PUBLIC_APP_DOMAIN` | Домен приложения | `http://localhost:3000` |
| `NEXT_PUBLIC_YM_ID` | ID Яндекс.Метрики | — |

### Порты

- **Разработка**: 3000 (фронтенд)
- **Бэкенд API**: 8001
- **Продакшн**: 80 (HTTP), 443 (HTTPS)

## 📚 Документация

- [Полное руководство по деплою](docs/DEPLOYMENT_GUIDE.md)
- [Краткая справка](docs/QUICK_REFERENCE.md)

## 🔧 Полезные команды

```bash
# Перезапуск контейнера разработки
docker-compose -f docker-compose.dev.yml restart frontend-dev

# Логи
docker-compose -f docker-compose.dev.yml logs -f frontend-dev

# Пересоздание контейнера (после изменения env)
docker-compose -f docker-compose.dev.yml up -d --force-recreate

# Остановка
docker-compose -f docker-compose.dev.yml down
```

