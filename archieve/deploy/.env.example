# Environment Configuration Template
# Скопируйте в .env.development или .env.production и заполните значения

# ===== API Configuration =====
# Client-side: относительный URL (проксируется через Next.js rewrites)
# Браузер будет делать запросы к /api/website/*
# Next.js автоматически проксирует на backend
NEXT_PUBLIC_API_BASE=/api/website

# Server-side: внутренний URL бекенда (только для сервера Next.js)
# Dev (backend на хосте): http://host.docker.internal:8000/api/website
# Dev (backend в Docker): http://backend:8000/api/website
# Prod: http://backend:8000/api/website
# ВАЖНО: Без префикса NEXT_PUBLIC_ (не попадает в браузер!)
API_INTERNAL_BASE=

# ===== Application =====
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=911
# Домен фронтенда (для canonical URLs и SEO)
NEXT_PUBLIC_APP_DOMAIN=http://localhost:3000
NEXT_PUBLIC_APP_ENV=development

# ===== Analytics =====
# Yandex Metrika ID
NEXT_PUBLIC_YM_ID=
# Google Analytics ID (опционально)
NEXT_PUBLIC_GA_ID=

# ===== Error Tracking (опционально) =====
NEXT_PUBLIC_SENTRY_DSN=

# ===== Feature Flags =====
NEXT_PUBLIC_FEATURE_REVIEWS=true
NEXT_PUBLIC_FEATURE_ANALYTICS=true
