# ===========================================
# Development Environment Configuration
# ===========================================

# Node environment
NODE_ENV=development

# Backend API URL (internal, server-side only)
# Для локального запуска (npm run dev) - используем localhost
# Для Docker (docker-compose.dev.yml) - используем host.docker.internal
API_INTERNAL_BASE=http://localhost:8001/api/website

# Public environment variables (available in browser)
NEXT_PUBLIC_API_BASE=/api/website
NEXT_PUBLIC_APP_DOMAIN=http://localhost:3000
NEXT_PUBLIC_APP_NAME=911
NEXT_PUBLIC_APP_ENV=development

# Development server port
PORT=3000
